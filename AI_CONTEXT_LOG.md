# AI Context Log

## 2026-07-14

- Project started as a new SMK Raja Shahriman digital portal.
- Approved a staff-first, public link hub for 9–15 external eSistem websites.
- Approved a cinematic 2.5D SMKRS robot hero using the official red, blue, yellow, and black crest palette.
- Approved a featured horizontal card rail plus searchable category directory.
- Approved local typed configuration for future system links; no database or admin panel in version one.
- Full design specification: `docs/superpowers/specs/2026-07-14-smkrs-digital-portal-design.md`.
- Bootstrapped a static React, TypeScript, and Vite application with Vitest and Testing Library.
- Foundation verification: 1 test passed, ESLint reported no errors, and the production build completed successfully.
- Task 3 preserved the official crest as a byte-identical public asset and added a typed `SystemCard` with safe external-link attributes, visible focus treatment, and reduced-motion behavior.
- Task 3 verification: the focused card test passed (1/1); the full suite passed (5/5), ESLint reported no errors, and the production build completed successfully.
- Task 4 generated the cinematic SMKRS robot with the built-in image tool using the crest only as a palette reference, then removed the flat chroma background into a validated RGBA asset at `public/mascot/smkrs-robot.png`.
- Added the session-only loader, accessible Malay hero, and reduced-motion-aware 2.5D robot scene with pointer parallax capped at 12 px.
- Task 4 verification: focused loader/hero tests passed (3/3), the full suite passed (8/8), ESLint reported no errors, and the production build completed successfully. Browser visual QA was unavailable because the Playwright CLI setup was aborted.
- Task 5 added the featured horizontal rail and complete eSistem directory without expanding the two-entry production configuration. The rail uses native overflow, scroll snap, focus/button activation, and a rail-rooted `IntersectionObserver`; the directory combines controlled Malay search and category filters with a live result count and resettable empty state.
- Task 5 verification: the required missing-component RED state was captured; focused discovery/App tests passed (7/7), the full suite passed (14/14), ESLint reported no errors, and the production build completed successfully.
- Task 5 review fix expanded the complete-directory fixture to 12 safe `example.com` links and corrected the tablet/mobile featured-card column to `minmax(0, 100%)`, preventing cards from exceeding the capped section width while retaining horizontal scroll snap.
- Review-fix verification: the new 12-card test passed immediately because rendering already supported arbitrary list sizes; focused directory/rail tests passed (6/6), the full suite passed (15/15), ESLint reported no errors, and the production build completed successfully.
- Task 6 added the final site footer with crest fallback, school identity, motto, the four supported categories, and a Malay notice that linked eSistem operate at their respective external destinations.
- Final responsive decisions: the hero stacks and the directory uses two columns at 768 px; the 390 px state uses a one-column directory, native horizontal swipe rail, minimum 44 px controls, and a lighter static mascot composition. Pointer translation is capped at 6 px for tablet-sized robot scenes and 12 px on desktop.
- Reduced-motion CSS removes smooth scrolling, loader/light-sweep animation, interactive transitions, and card transforms; the React composition also skips the loader and hero/mascot motion when `prefers-reduced-motion` is active.
- Missing crest and mascot images are hidden on load error; the footer retains a branded `SMKRS` fallback so a missing asset does not break the composition.
- Task 6 TDD evidence: `npm.cmd run test -- src/App.integration.test.tsx` first failed because the external-destination footer notice was absent, then passed (1/1) after the completed composition was added.
- Task 6 automated verification on 2026-07-14: `npm.cmd run test` passed 16/16 tests across 8 files; `npm.cmd run lint` completed with zero errors; `npm.cmd run build` completed with Vite 8.1.4 and generated `dist/`.
- Task 6 browser QA was not completed. The attempt to start the local Vite app for in-app-browser inspection was interrupted before a usable browser session or viewport evidence was obtained; no browser tooling was installed or retried. Desktop 1440×900, tablet 768×1024, mobile 390×844, keyboard, reduced-motion, rail interaction, missing-image, and 200% zoom checks therefore remain release-gate work.
- Task 6 review fix removed the mobile rail's horizontal-only `touch-action` so native horizontal swipe remains available without blocking vertical page panning, and now routes a mascot load failure through the loader's idempotent completion path immediately.
- Review-fix TDD captured both regressions before implementation; focused tests passed 3/3, the full suite passed 18/18 across 9 files, ESLint reported no errors, and the production build completed successfully with Vite 8.1.4.
- Browser fallback fix added an explicit `img[hidden] { display: none; }` cascade rule so failed mascot and crest assets remain hidden despite the global block-image reset. TDD captured the missing rule first; focused tests passed 2/2, the full suite passed 19/19 across 9 files, ESLint reported no errors, and the production build completed successfully with Vite 8.1.4.
- Final browser QA used local Microsoft Edge after the in-app browser runtime failed to initialise (`Cannot redefine property: process`). Exact 1440 x 900, 768 x 1024 and 390 x 844 emulated viewports showed no horizontal overflow and matched the intended desktop, two-column tablet and one-column mobile layouts. Search empty state, reset, category filtering, safe external-link attributes, six-step keyboard focus visibility, reduced motion, missing-image removal, and a 200% zoom-equivalent viewport all passed; the three rendered screenshots were also inspected visually.
- Final storage hardening makes the loader fail open when `sessionStorage` access is unavailable: read failures skip the loader, while write failures cannot interrupt hiding it or calling `onComplete`. Focused TDD first captured both thrown-storage regressions, then passed 4/4; the full suite passed 21/21 across 9 files, ESLint reported no errors, and the production build completed successfully with Vite 8.1.4.

## 2026-07-14 (rebuild)

- The user rejected the SaaS-style landing page and requested a full teardown modeled on `reference movie/TikSave.io_7648552705841040674.mp4`: a cinematic single-screen opening with a percentage loader, full-bleed sky hero, oversized display typography, a floating pill navbar, an infinite tilted-card marquee, particles, and a day/night scene toggle.
- Approved decisions: no robot mascot; pure-code animated sky (CSS cloud layers + canvas stars/particles) with a replaceable `heroImage` slot in `src/data/site.ts` for future school photos; crest gold as the primary accent on deep dusk/night backgrounds; single screen only (no scroll sections); manual day/night toggle in the navbar.
- Removed `HeroSection`, `RobotScene`, `FeaturedRail`, `SystemDirectory`, `SystemCard`, `SiteFooter`, `PortalLoader`, `lib/systems.ts`, their tests, and `public/mascot/`. Kept the `SystemEntry` contract and config-driven `src/data/systems.ts`.
- New composition: `Loader` (eased 0→100% gold counter, session-once, storage fail-open, reduced-motion skip), `SkyScene` (crossfading day/night gradients, drifting blurred cloud layers with pointer parallax, sun/moon orb, canvas stars plus falling leaves↔snow that swap organically on theme change), `NavBar` (pill with crest badge and theme toggle), `HeroTitle` (staggered reveal: eyebrow, gold SERASHA with sparkles, outlined school name, Caveat motto, tagline), `SystemMarquee` (config-driven infinite marquee, duplicate copy aria-hidden, hover/focus pause, static scrollable under reduced motion).
- Theme: `data-theme` on the root element, initial theme from `?theme=` URL param → `localStorage` → local clock (07:00–19:00 = day); a pre-paint inline script in `index.html` prevents theme flash. Self-hosted fonts via @fontsource (Space Grotesk Variable, Caveat).
- Loader bug found by tests: writing the session flag on completion flipped a render-time `shouldSkip` recomputation and unmounted the loader before its exit animation; fixed by evaluating `shouldSkip` once in a `useState` initializer.
- Verification on 2026-07-14: 12/12 tests across 3 files, ESLint clean, production build green. Visual QA via the in-app browser pane (desktop day theme with full hero, night toggle working, marquee cards linking out) plus headless Edge screenshots of day/night at 1440×900 and 390×844. Headless Edge quirks noted: framer-motion reveals do not advance under `--virtual-time-budget`, and its ~500px minimum window width makes 390px captures right-cropped; both were confirmed fine in the real browser pane.

## 2026-07-14 (iteration 2: drag rail + four sky moods)

- Replaced the infinite marquee with a drag rail (`SystemRail` supersedes `SystemMarquee`): mouse drag scrolls via pointer capture with a 5px threshold that suppresses the click ending a drag, touch uses native scrolling, the scrollbar is hidden, and each system renders exactly once (no duplicated loop copy).
- Expanded two themes into four sky moods cycling night → dawn → day → dusk → night. The old warm "day" palette became `dawn`; `day` is a new bright blue sky (dark navy text, light glass navbar/cards, amber gold overrides for contrast); `dusk` is a deeper orange-red sunset with a low sun. New CSS vars `--outline`, `--hairline`, `--scrim-top`, `--shadow-ink` carry the light-on-dark vs dark-on-light differences.
- Theme preference is now `auto | dawn | day | dusk | night` (`src/lib/theme.ts`, which now owns the `Theme` type). Auto is the default: `themeForTime` maps dawn 6:00–9:00, day 9:00–17:00, dusk 17:00–19:30, else night, and an interval re-checks every 30s so the sky switches by itself. The navbar button cycles Auto→Subuh→Siang→Senja→Malam with per-state lucide icons; the pre-paint script in `index.html` mirrors the same mapping.
- Verification: 14/14 tests across 3 files, ESLint clean, build green. Headless Edge `--force-prefers-reduced-motion` discovered as the workaround for the framer-motion/virtual-time artifact: full-hero screenshots of all four themes confirmed day-theme contrast (amber title, navy outline and tagline on bright sky). Live mouse-drag feel not yet exercised in a real browser (pane unresponsive); drag click-suppression is unit-tested.

## 2026-07-14 (iteration 3: mobile performance + Semakan ID DELIMa)

- Mobile jank diagnosis: the main costs were per-frame `backdrop-filter` sampling on the navbar and every rail card, the particle canvas rendering at devicePixelRatio 2, and large cloud blurs. Not primarily a device fault.
- Fixes: on ≤768px screens backdrop-filter is removed and replaced with an `inset 0 0 0 999px` self-tint shadow to keep contrast; the canvas caps DPR at 1.25 with fewer stars (width/14) and 10 particles; cloud puffs 5+ per layer are hidden and blur radii roughly halved; the orb keeps a single smaller glow; cloud spans gained `will-change: transform`.
- Added the third eSystem entry `semakan-id-delima` (Semakan ID DELIMa, https://e-delimaserasha.netlify.app/, green accent, `file-text` icon) via `src/data/systems.ts` only — no component changes needed.
- Session resume on 2026-07-14: prior agent hit a usage limit before verification. Re-ran checks in this session — `npm run test` 14/14 across 3 files, `npm run lint` clean, `npm run build` green (Vite 8.1.4). No further code changes required.
- Theme persistence removed: every visit defaults to time-based `auto` (URL `?theme=` still overrides). Manual navbar cycling is session-only; no `localStorage` for theme.
- Added a clear digital HUD veil over the sky (intensity B): crest-gold/navy theme-aware mesh + perspective floor grid, scan sweep, dual tech rings on the sun/moon orb, and a light node-link canvas. Mobile uses coarser grid and fewer nodes; reduced-motion freezes orbit/scan and draws a static network.
- Bugfix: card clicks failed because drag mode applied `pointer-events: none` and pointer capture on every mouse down. Capture and the dragging class now start only after the 5px drag threshold so plain clicks open links again.
